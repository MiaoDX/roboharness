<!-- Generated template. Copy before editing a project-specific brief. -->
# Harness Scope Brief: Roboharness

## Proposed Scope Change

- Request:
- Why the existing contract is insufficient:
- Proposed semantic phases:
- Proposed hard metric gates:
- Proposed visual review dimensions:
- Proposed evidence boundaries:
- Proposed validation commands:

## User Approval Gate

Do not update `contract.py` or generated artifacts until the user approves the scope change. After approval, edit `contract.py`, regenerate the skill, and run `roboharness contract check contract.py --output-dir .`.
